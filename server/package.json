{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "server": "bun --hot --env-file=./../.env index.ts",
    "watch": "bun --watch index.ts",
    "migrate": "dotenv -e ./../.env -- prisma migrate dev"

  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^5.9.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.9.1",
    "dotenv-cli": "^7.3.0"
  }
}